// objects and arrays are so called reference types

const friends = ['Ram', 'Shyam', 'Gita'];

friends.push('ms');
console.log(friends);


// it only changes the pointer not the friends, as all are called reference Types.
